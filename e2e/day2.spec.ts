import { test, expect } from '@playwright/test';
test('NotifyFlow Day 2 fixture happy path through API', async ({ page }) => {await page.goto('/');await page.getByRole('button',{name:'Run demo fixture instead'}).click();await expect(page.getByText('Grounded analysis')).toBeVisible();await expect(page.getByLabel('analysis mode')).toContainText('Demo fixture');for(const name of [/100 requests\/sec/,/At-least-once/,/Metadata 90 days/,/Tenant ID from authenticated token/]) await page.getByRole('button',{name}).first().click();await expect(page.getByText('Unlocked')).toBeVisible();await expect(page.getByText('Transparent weighted comparison')).toBeVisible();await page.getByRole('button',{name:'Explicitly approve selected option'}).click();await expect(page.getByText(/Versioned ADR ADR-001/)).toBeVisible();});
