export default function ModelsLoading() {
  return <main className="account-shell"><section className="account-card model-catalog-card account-state" role="status" aria-live="polite" aria-busy="true"><span className="landing-kicker"><i /> Loading</span><h1>Loading model policy…</h1><p>Axiom is reading the current tenant-scoped provider lifecycle and tier assignments.</p></section></main>;
}

