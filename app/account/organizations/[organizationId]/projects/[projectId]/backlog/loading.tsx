export default function BacklogLoading() {
  return <main className="account-shell"><section className="account-card backlog-review-card" aria-busy="true"><span className="landing-kicker"><i /> Loading</span><h1>Loading backlog preview…</h1><p>The current persisted draft and quality evidence are being read from the platform.</p></section></main>;
}
