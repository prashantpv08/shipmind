import { NextResponse } from 'next/server';
import { providerForEnv } from '../../../src/ai/provider';
import { AnalyzeRequest } from '../../../src/domain/schemas';
export async function POST(request:Request){let json:unknown;try{json=await request.json();}catch{return NextResponse.json({error:'Request body must be valid JSON'},{status:400});} const parsed=AnalyzeRequest.safeParse(json); if(!parsed.success)return NextResponse.json({error:parsed.error.issues[0]?.message??'Invalid request body'},{status:400}); try{const result=await providerForEnv(parsed.data.useFixture).analyze(parsed.data.brief); return NextResponse.json(result);}catch(e){return NextResponse.json({error:e instanceof Error?e.message:String(e),run:{label:'Live AI failed · no fixture substituted',providerName:'openai-responses',modelName:process.env.OPENAI_MODEL||'gpt-5.6-sol',mode:'live',startedAt:new Date().toISOString(),completedAt:new Date().toISOString(),outcome:'FAILED'}},{status:502});}}
